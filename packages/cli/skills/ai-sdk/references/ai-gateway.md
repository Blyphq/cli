# AI Gateway

- Prefer the Vercel AI Gateway unless the user explicitly requests a different provider path.
- Fetch current model IDs before writing code that references a model.
- Keep provider configuration minimal and aligned with current docs.
