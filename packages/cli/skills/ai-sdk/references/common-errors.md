# Common Errors

- `parameters` was renamed to `inputSchema` in newer agent/tool APIs.
- `useChat` changed substantially; verify current docs before writing client code.
- Prefer checking `node_modules/ai/docs/` and `node_modules/ai/src/` before relying on examples.
